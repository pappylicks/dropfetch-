{
  "name": "dropfetch",
  "version": "1.1.0",
  "private": true,
  "description": "Local media downloader with automatic yt-dlp and FFmpeg bootstrap on Windows.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.0"
  }
}
